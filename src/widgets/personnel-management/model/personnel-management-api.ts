import {
  createPersonnel,
  deletePersonnel,
  listPersonnel,
  updatePersonnel,
} from "@/entities/personnel/api/personnel"

export const personnelManagementApi = {
  createPersonnel,
  deletePersonnel,
  listPersonnel,
  updatePersonnel,
}
