import {
  createPersonnel,
  listPersonnel,
  updatePersonnel,
} from "@/entities/personnel/api/personnel"

export const personnelManagementApi = {
  createPersonnel,
  listPersonnel,
  updatePersonnel,
}
